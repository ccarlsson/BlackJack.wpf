<Window x:Class="BlackJack.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:BlackJack.Presentation.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="500"
        Width="900">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}"
                   FontSize="28"
                   FontWeight="SemiBold"/>

        <Border Grid.Row="1"
                Background="#0F000000"
                CornerRadius="12"
                Padding="12"
                Margin="0,12,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Player" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding PlayerName}" Width="180" Margin="12,0,0,0"/>
                    <TextBlock Text="Round:" Margin="24,0,0,0" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding RoundStateText}" Margin="8,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Status:" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding StatusText}" Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#0F000000" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Player hand" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding PlayerValue, StringFormat=Value: {0}}" Margin="0,6,0,0"/>
                    <ItemsControl ItemsSource="{Binding PlayerCards}" Margin="0,8,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="0,2,0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#0F000000" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="Dealer hand" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding DealerValueText}" Margin="0,6,0,0"/>
                    <ItemsControl ItemsSource="{Binding DealerCards}" Margin="0,8,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="0,2,0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    Margin="0,16,0,0">
            <Button Content="New round"
                    Command="{Binding NewRoundCommand}"
                    Padding="16,8"
                    Margin="0,0,12,0"/>
            <Button Content="Hit"
                    Command="{Binding HitCommand}"
                    Padding="16,8"
                    Margin="0,0,12,0"/>
            <Button Content="Stand"
                    Command="{Binding StandCommand}"
                    Padding="16,8"
                    Margin="0,0,12,0"/>
            <Button Content="Double down"
                Command="{Binding DoubleDownCommand}"
                Padding="16,8"
                Margin="0,0,12,0"/>
            <Button Content="Split"
                Command="{Binding SplitCommand}"
                Padding="16,8"
                Margin="0,0,12,0"/>
            <Button Content="Finish round"
                    Command="{Binding FinishRoundCommand}"
                    Padding="16,8"/>
        </StackPanel>
    </Grid>
</Window>
