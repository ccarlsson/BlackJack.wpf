<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="CardTemplate">
        <Border x:Name="CardFrame" Background="#1A2432" BorderBrush="#2D3B50" BorderThickness="1" CornerRadius="8" Padding="8" Margin="0,2,0,2">
            <TextBlock x:Name="CardLabel" Text="{Binding}" FontFamily="Consolas" FontSize="13" Foreground="{StaticResource TextPrimary}"/>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding}" Value="Hidden">
                <Setter TargetName="CardFrame" Property="Background">
                    <Setter.Value>
                        <DrawingBrush TileMode="Tile" Viewport="0,0,12,12" ViewportUnits="Absolute" Viewbox="0,0,12,12" ViewboxUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <GeometryDrawing Brush="#1B1F2A" Geometry="M0,0 L12,0 12,12 0,12 Z"/>
                                    <GeometryDrawing Brush="#233044" Geometry="M-6,12 L12,-6 18,0 0,18 Z"/>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Setter.Value>
                </Setter>
                <Setter TargetName="CardFrame" Property="BorderBrush" Value="#3B4A66"/>
                <Setter TargetName="CardLabel" Property="Text" Value="Card back"/>
                <Setter TargetName="CardLabel" Property="Foreground" Value="{StaticResource TextMuted}"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>
